/* import PropTypes from 'prop-types';
import { filmTrend } from '../../../database/filmTrend';

const Filmtrend = ({ filmTrend }) => {
     return (
          <div className='filmtrend'>
               <img src="{filmTrend.image}" alt="trend" className="trend-img" />
               {filmTrend.title}
			{filmTrend.year}
			{filmTrend.genre}
               {filmTrend.time}
			{filmTrend.description}
          </div>
     )
}

Filmtrend.propTypes = {filmTrend: PropTypes.object,};
 */